[package]
name = "yourails_rust"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.45" # cargo add chrono
image = "0.25.10"
printpdf = { version = "0.12", features = ["images", "png", "jpeg", "webp", "tiff"] }
